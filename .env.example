# Database Configuration
# Supported types: sqlite, mysql
DB_TYPE=sqlite
DB_NAME=database.db

# MySQL Database Configuration (only used if DB_TYPE=mysql)
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=your_password_here

# Flask Configuration
SECRET_KEY=your_secret_key_here

